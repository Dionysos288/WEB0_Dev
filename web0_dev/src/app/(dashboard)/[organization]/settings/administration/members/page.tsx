import type { Metadata } from 'next';

export async function generateMetadata(): Promise<Metadata> {
	return {
		title: `Members `,
		description: `View Members on Web0`,
	};
}
const page = async () => {
	

	return();
};

export default page;
