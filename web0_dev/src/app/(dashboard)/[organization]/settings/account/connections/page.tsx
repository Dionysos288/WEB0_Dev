import type { Metadata } from 'next';

export async function generateMetadata(): Promise<Metadata> {
	return {
		title: `Connections `,
		description: `View Connections on Web0`,
	};
}
const page = () => {
	return <></>;
};

export default page;
